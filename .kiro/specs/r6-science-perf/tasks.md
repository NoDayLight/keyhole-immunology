# R6 science-performance tasks

- [x] Add exact batched one-hot/matmul foreignness and default pipeline batching with scalar injection compatibility.
- [x] Integer-code seeded HLA genotypes and precompute per-allele carrier masks without changing coverage.
- [x] Cache immutable peptide encodings per frozen binder and immutable famous-protein source records per data root.
- [x] Reuse the pipeline's single validation through private trusted CLI serializers while preserving public validation.
- [x] Add focused regression tests for exact equivalence, cache safety, and validation ownership.
- [x] Prove fixed-epoch SKCM JSON byte identity and run the full R6 boundary checks.
- [x] Append the R6 decision entry and commit one green boundary.
